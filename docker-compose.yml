services:
  server:
    build: ./server
    ports: ["8080:8080"]
    # TODO: Add an healthcheck to about.json

  #TODO: Add a client_mobile

  client_web:
    build: ./web
    depends_on:
      # -client_mobile
      - server
    ports: ["8081:8081"]
    volumes:
      - apkshare:/app/public

volumes:
  apkshare:
